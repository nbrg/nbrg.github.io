---
article_class: bout
---
<% against = data.leagues[bout.against] %>
<h1><%= bout.nickname %></h1>
<%= date_tag(bout.datetime) %>

<section class="scoreboard">
  <div class="team <%= bout.won ? bout.won == 'us' ? 'winner' : 'loser' : '' %>">
    <h2><span>Northern Beaches Roller Girls</span></h2>
    <img src="/images/leagues/nbrg.png" width="128" />
    <% if bout.score %>
      <p><%= bout.score.us %></p>
    <% end %>
  </div>

  <div class="team <%= bout.won ? bout.won == 'them' ? 'winner' : 'loser' : '' %>">
    <h2><span><%= against.name %></span></h2>
    <img src="/images/leagues/<%= against.nickname.downcase %>.png" width="128" />
    <% if bout.score %>
      <p><%= bout.score.them %></p>
    <% end %>
  </div>
</section>

<% if bout.venue %>
  <section class="venue" data-map data-lat="<%= bout.venue.lat %>" data-lon="<%= bout.venue.lon %>">
    <h2><span><%= bout.venue.name %></span></h2>
    <p><%= bout.venue.address %></p>
  </section>
<% end %>
